<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@color/APP_MAIN_BACKGROUND"
    android:padding="10dp"
    android:gravity="center"
    android:orientation="vertical">

    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/content_image_second"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="10dp"
        android:src="@drawable/blink_smaller" />

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/second_intro_text"
        android:layout_toEndOf="@+id/content_image_second"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:maxEms="10"
        tools:text="@string/content_second_intro"
        android:textSize="17sp" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/buttonContinue"
        android:layout_width="105dp"
        android:layout_height="40dp"
        android:layout_gravity="start"
        android:layout_marginTop="12dp"
        android:textSize="12sp"
        android:insetBottom="0dp"
        android:insetLeft="0dp"
        android:insetRight="0dp"
        android:insetTop="0dp"
        android:layout_toEndOf="@+id/content_image_second"
        app:cornerRadius="8dp"
        android:layout_below="@+id/second_intro_text"
        android:text="@string/continue_button"/>

</RelativeLayout>